/** The shell's one horizontal pill shape -- shared by the Window Carousel and every utility pill flanking it (Notifications, the clock), so a new one never invents its own shape. Pair with SURFACE_BG (Gradient to Contrast) for the fill. */
export const UTILITY_PILL_CLASSES = "inline-flex h-10 shrink-0 items-center gap-1 rounded-[var(--app-corner-radius,16px)] px-2";
